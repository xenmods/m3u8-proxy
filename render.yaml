services:
  - type: web
    name: simple-proxy
    env: standard
    buildCommand: npm install
    startCommand: npx ts-node src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
    healthCheckPath: /health
    plan: free
    runtime: node
